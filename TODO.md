* To dos

1. install docker [x]
2. install docker composer [x]
3. dockerize [x]
4. add database tables [x]
4. - **Files** [x]
5. - **FileMetadata** [x]
6. - **Tags** [x]
8. - **Collections/Playlists** [x]
9. upload a file via UI [x]
10. store file to disk `/data/library/...` [x]
11. extend input to the following types
    - pdf
    - epub
    - jpg
    - png
    - mp3
    - docx
    - txt [x]
12. store file path to database [x]
13. store metadata to database, first
only for the pdf
14. if there is another file with the same name, make sure to add a second or third one with a number appended to the file name [x]
15. refactor first upload logic
16. show uploaded files in a list in the UI [x]
17. two buttons, view to view in browser, and delete to delete the file completely, update the UI accordingly on deletion [x]
18. view the file in the browser [x]
19. style the upload form [x]
20. upload all other file types and create subfolders for each type [x]
21. view epubs in the browser [x]
22. fix uploading removes all files from the UI

